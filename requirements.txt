# Sem dependencias externas — apenas Python stdlib!
# Nota: No Windows, a interface curses precisa de:
#   pip install windows-curses
# Senao, o cliente cai automaticamente para o modo console (sem curses).
